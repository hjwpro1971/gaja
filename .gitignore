# NAS(Y:\gaja-tools) 전용 산행가이드 제작 도구. 이 저장소는 GitHub Pages로
# 공개되므로, 도구 소스를 여기 커밋하면 그대로 공개된다. NAS에만 두고
# 저장소에는 올리지 않는다.
rrkwkrhghdwndnjs/

gh_token.js

# 구버전 뷰어 — track_viewer.html로 통합됨. 로컬(및 NAS)에는 남겨두되
# GitHub Pages로 공개되는 이 저장소에는 올리지 않는다.
viewer.html
viewer1.html
viewer_pc.html
